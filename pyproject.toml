[project]
name = "naijatax-offline"
version = "0.1.0"
description = "ADTC 2026 offline Nigerian tax model and verification pipeline"
requires-python = ">=3.12"
dependencies = [
    "pypdf>=5.0",
]

[tool.uv]
package = false
